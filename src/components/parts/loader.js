import React from "react";
import "./loader.css"; // Assuming you have a CSS file for the loader styles

const Loader = () => {
  return <div className="loader"></div>;
};

export default Loader;
